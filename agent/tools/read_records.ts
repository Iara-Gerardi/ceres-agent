import { defineTool } from 'eve/tools';
import { z } from 'zod';
import { getRuntime } from '../../core/runtime.ts';
import { id } from '../../core/contracts.ts';
import { usable } from '../../core/validation.ts';
export default defineTool({description:'Inspect saved demo records, experiment proposals, claims, evidence, research gaps, conclusions, failures, and revision history. For reasoning, use current_only=true. Experiments are historical proposals; check their linked hypothesis before reusing them.',inputSchema:z.object({id:id.optional(),kind:z.enum(['insight','hypothesis','experiment','claim','research_gap','finding','fetched_document','evidence_assessment','research_assessment','research_conclusion','analytics','run','failure','research_decision','research_stop','provider_attempt','workflow_state']).optional(),current_only:z.boolean().default(true),history:z.boolean().default(false)}).strict(),async execute(input) {const {store}=await getRuntime(); if(input.id) {const record=await store.get(input.id); return {record:input.current_only && ['insight','hypothesis'].includes(record.kind) && !usable(record) ? null : record,history:input.history ? await store.history(input.id):[]};} return (await store.list(input.kind)).filter(d => !input.current_only || !['insight','hypothesis'].includes(d.kind) || usable(d));}});
